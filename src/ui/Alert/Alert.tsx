'use client'

import React from 'react';
import { ToastContainer } from 'react-toastify';

const Alert: React.FC = () => {
  return <ToastContainer position='top-right' hideProgressBar />;
};

export default Alert;
